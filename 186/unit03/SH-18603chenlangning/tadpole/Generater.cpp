#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("tadpole.in", "w", stdout);
    srand(time(NULL));
    cout << (rand() % 1001) << " " << (rand() % 1001) << endl;
    return 0;
}
