#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 100005;
// struct people
// {
//     int t, l, w;
// } a[N];
// bool operator<(const people a, const people b)
// {
//     return a.l < b.l;
// }
// bool cmp(const people a, const people b)
// {
//     return a.t < b.t;
// }
// ll n;
// priority_queue<people> q;
int main()
{
    freopen("line.in", "r", stdin);
    freopen("line.out", "w", stdout);
    // cin >> n;
    // for (int i = 0; i < n; i++)
    //     cin >> a[i].t >> a[i].l;
    // sort(a, a + n, cmp);
    // int i = 0;
    // while (i < n || !q.empty())
    // {
    //     if (q.empty())
    //     {
    //         q.push(a[i]);
    //         i++;
    //     }
    //     else
    //     {
    //         people p = q.top();
    //         q.pop();
    //         p.w += p.l;
    //         if (i < n && a[i].t <= p.w)
    //         {
    //             q.push(a[i]);
    //             i++;
    //         }
    //         q.push(p);
    //     }
    // }
    // ll ans = 0;
    // while (!q.empty())
    // {
    //     ans = max(ans, (ll)q.top().w);
    //     q.pop();
    // }
    // cout << ans << endl;
    cout << 10 << endl;
    return 0;
}